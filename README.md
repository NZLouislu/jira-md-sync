# jira-md-sync

Sync your Jira issues into Markdown files.  
Part of the `*-md-sync` open-source series for unifying project management data into Markdown.

---

## ✨ Features
- Export Jira issues, epics, and sprints into Markdown
- Preserve metadata (status, assignee, labels, due dates)
- CLI-friendly: run with a single command
- Ideal for documentation, reporting, and AI workflows

---

## 🚀 Installation
```bash
npm install -g jira-md-sync
