# Jira Connection Configuration
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token-here
JIRA_PROJECT_KEY=PROJ
JIRA_ISSUE_TYPE_ID=10001

# Directory Configuration (Optional)
# ==========================================
# Input Directory: Where you edit markdown source files
# Default: jiramd
MD_INPUT_DIR=jiramd

# Output Directory: Where Jira exports are saved (sync cache)
# Default: jira
MD_OUTPUT_DIR=jira

# Status Mapping (Optional)
# ==========================================
# Custom status mapping in JSON format
# Maps your markdown status names to Jira status names
# If not set, uses default mapping:
#   Backlog, To Do, Ready → Backlog
#   In Progress → In Progress
#   In Review → In Review
#   Done → Done
# STATUS_MAP={"To Do":"Backlog","Code Review":"In Review","Closed":"Done"}

# Dry Run Mode (Optional)
# ==========================================
# Set to "true" to preview changes without executing
# DRY_RUN=false

# JQL Query (Optional)
# ==========================================
# Custom JQL for jira-to-md export
# If not set, exports all issues from the project
# JIRA_JQL=project = PROJ AND status = "In Progress" ORDER BY created DESC

# Notes:
# ==========================================
# 1. Get API token: https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Find Issue Type ID: Project Settings > Issue Types > Click issue type > Check URL
# 3. Input directory (jiramd/): Your source files, commit to Git
# 4. Output directory (jira/): Sync cache, add to .gitignore
